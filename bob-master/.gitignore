# Prepared per docs/PROJECT-BRIEF-FOR-NEW-DEV.md §4/§11 — not yet wired to a repo.
# Secrets are plaintext files on the source Mac mini; never let copies get committed.
*credentials*
*.env
.env.*
!.env.example

# Call recordings / transcripts — large, sensitive, not needed to rebuild logic
*transcripts*/
*.m4a

# Stale per-run scratch dirs created by copy-forked scripts (eod_0720/, week_0714/, etc.)
eod_*/
day_*/
week_*/

.DS_Store

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

